# Expected Failures

Tests in `testbed/tests/audit/` are tagged here with their current
expected outcome. Any PR that changes a row — especially flipping
a test from `EXPECTED-FAIL` to `RESOLVED` — must attach the
before/after transcripts to the PR description.

## Status legend

- **EXPECTED-FAIL** — the test is currently expected to fail; this
  matches an unresolved BLOCKER or MAJOR finding from the audit.
- **RESOLVED** — the fix has landed, the test passes.
- **BLOCKED-BY-INTEROP** — the test cannot be run yet because an
  upstream peer is not yet available in the container (NLSR,
  ACME, etc.).
- **NOT-WITNESSABLE** — a feature-absence or policy-shape finding
  that has no corresponding observable packet event until a concrete
  runtime behavior is implemented.

Last reviewed: 2026-05-28. The tracked release-audit harness now derives
its script list and expectations from this file; local run
`RESULTS_DIR=/private/tmp/ndn-audit-results bash testbed/tests/audit/run_all.sh`
reported 54 PASS / 0 FAIL / 0 SKIP, with 0 divergences. The Docker interop
harness has also been run against the current testbed image;
`testbed/tests/interop/run_all.sh` reports 8 passed, 0 failed, 0 skipped
after moving the NDNts runtime to Node 24. The tracked audit transcript is
`testbed/tests/audit/transcripts/release_audit_run_all_after.txt`. See
`docs/notes/spec-compliance-cross-reference-2026-05-01.md` for the ndn-cxx /
NFD / ndnd / ndn-svs source-line citations and
`docs/notes/spec-compliance-scrape-and-witness-plan-2026-05-27.md` for the
live scrape/reverification notes.

The 2026-05-01 cross-reference also retracted four findings (A.17,
C.04, B.03, B.04) and added thirteen new entries (N.01–N.14); the
new rows appear below under the appropriate severity tiers.

## BLOCKER

| Finding | Witness test | Status | Predicted failure | Last seen |
|---------|------|--------|----|-|
| A.01 | `a01_blake3_name_component.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | BLAKE3_DIGEST 0x03 name-component semantics removed: TLV-TYPE 3 now round-trips as an opaque typed component without a `blake3digest=` URI alternate. Deleted ZoneKey / zone-bound DID surfaces remain documented extraction history, not release proof. | 2026-05-28 (rust-unit witness) |
| A.09 | `a09_signed_interest_verify.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | Signed Interest construction now has a direct Rust witness: ndn-packet records the bytes handed to the signer and proves they equal `Interest::signed_region()` after decoding the final wire; ndn-security proves KeyChain-signed Interests verify against that region. | 2026-05-28 (rust-unit witness) |
| B.01 | `b01_reliability_txsequence.sh` | RESOLVED 2026-05-01 | encoder writes `TxSequence` (0x0348), decoder + ack-extractor read `0x0348`, reliability layer keys `unacked` on TxSequence; witness flipped to RUST-UNIT (`b01_b09_reliable_wire_uses_tx_sequence` + `b01_b09_fragmented_reliable_carries_both_sequences`). Before/after transcripts at `testbed/tests/audit/transcripts/b01_{before,after}.txt`. | 2026-05-01 (rust-unit witness) |
| B.09 | rolled into `b01_reliability_txsequence.sh` | RESOLVED 2026-05-01 | fragmented frames now carry both `Sequence` (0x51) shared across fragments and per-LP `TxSequence` (0x0348); `LpReliability` splits `next_seq` (network-packet) from `next_tx_seq` (per-LP). | 2026-05-01 |
| C.01 | `c01_ecdsa_verify.sh` / `c01_rsa_ecdsa_verifiers.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | Validator dispatches via `verify_by_sig_type` per `SignatureType`. DigestSha256, Ed25519, HmacSha256, Blake3-plain/keyed, RSA-SHA256, and ECDSA-SHA256 all have behavioral verifier witnesses; RSA/ECDSA cover valid signature, wrong signature, malformed key, and validator dispatch. | 2026-05-28 (rust-unit witness) |
| C.07 | `c07_cert_naming.sh` | RESOLVED 2026-05-01 | KeyChain ephemeral now produces cert names ending `/KEY/<keyid>/<issuer>/<version>` per ndn-cxx Certificate::isValidName. Witness: RUST-UNIT in `ndn-security` integration tests/cert_format.rs. Before/after transcripts at `testbed/tests/audit/transcripts/c07_{before,after}.txt`. | 2026-05-01 |
| C.08 | `c08_cert_content.sh` | RESOLVED 2026-05-01 | cert Content body is DER SubjectPublicKeyInfo (44-byte Ed25519 envelope per RFC 8410); `encode_cert_data` wraps via `ndn_security::spki::wrap_ed25519`; `Certificate::decode` unwraps. Before/after transcripts at `testbed/tests/audit/transcripts/c08_{before,after}.txt`. | 2026-05-01 |
| C.09 | `c09_safebag_ndnsec_interop.sh` | RESOLVED 2026-05-28 (LIVE-INTEROP) | SafeBag portability is now cross-implementation witnessed: ndn-rs exports an ECDSA-P256 SafeBag, the script base64-armors it for reference `ndnsec import`, `ndnsec export` re-emits the imported identity, and ndn-rs decrypts/verifies the re-exported SafeBag by checking the private key's public key against the embedded CertificateV2. The fix also made SafeBag encryption explicitly PBES2/PBKDF2-HMAC-SHA256/AES-256-CBC instead of the RustCrypto scrypt default that ndn-cxx rejects. | 2026-05-28 |
| C.13 | `c13_ndncert_challenge_tlv.sh` / `c13_ndncert_live_interop.sh` | RESOLVED 2026-05-28 (RUST-UNIT + INTEROP) | CHALLENGE plaintext is TLV-encoded ParameterKey (0x85) / ParameterValue (0x87) pairs per NDNCERT 0.3 §2.4.3. The live interop witness now runs ndn-rs `enroll-ndncert` against upstream `ndncert-ca-server`, completes NEW plus two-round PIN CHALLENGE, fetches the issued Certificate v2 Data, decodes it with ndn-rs, and verifies the issuer chains under `/test/ndncert/CA`. | 2026-05-28 |
| C.18 | `c18_validity_period_iso8601.sh` | RESOLVED 2026-05-01 | NotBefore/NotAfter are 15-byte ASCII `YYYYMMDDTHHMMSS` strings per ndn-cxx ISO_DATETIME_SIZE=15; ValidityPeriod relocated from Content to SignatureInfo. New `ndn_security::iso8601` helper. Before/after transcripts at `testbed/tests/audit/transcripts/c18_{before,after}.txt`. | 2026-05-01 |
| D.01 | `d01_hoplimit_decrement.sh` | RESOLVED 2026-05-28 (RUST-UNIT + INTEROP) | `ndn_packet::interest::decrement_hop_limit` rewrites the HopLimit byte in-place; `TlvDecodeStage::decode_interest` decrements after the zero-check per NFD `daemon/fw/forwarder.cpp:104-111`. The Docker live witness routes `/d01-probe` through ndn-fwd to NFD: HopLimit=2 reaches the NFD producer after decrement, while HopLimit=1 times out after ndn-fwd drops the post-decrement zero-hop Interest. | 2026-05-28 |
| E.04 | `e04_dataset_segmentation.sh` | RESOLVED 2026-05-28 (RUST-UNIT + INTEROP) | `send_dataset` wraps responses into versioned segments via `build_segmented_dataset` per ndn-cxx `mgmt/status-dataset-context.cpp` (`<base>/v=<v>/seg=<n>`, `MAX_DATASET_PAYLOAD_LEN = 8000`, FinalBlockId on the last segment). The Docker live witness fetches `/localhost/nfd/faces/list` through ndn-fwd and verifies the returned Data name carries version+segment components and `final-block-id`. | 2026-05-28 |
| N.01 | `n01_fragcount_dos.sh` | RESOLVED 2026-05-01 | New `MAX_FRAGMENTS = 400` cap in `ReassemblyBuffer::process` rejects oversized FragCount before any allocation, matching NFD `daemon/face/lp-reassembler.hpp:52-56` (`nMaxFragments = 400`). Two RUST-UNIT tests cover the cap and the at-limit case. Before/after transcripts at `testbed/tests/audit/transcripts/n01_fragcount_dos_{before,after}.txt`. | 2026-05-01 |
| E.01 | `e01_mgmt_unauth.sh` | RESOLVED 2026-05-01 (architecture; default-on flip + trust anchors deferred) | New `authorize_command` gate in `mgmt_ndn.rs` rejects unsigned commands when `MgmtHandles::require_signed_commands=true`; the gate uses `Validator::validate_interest` (audit C.11). Architecture wired; default is still off so existing deployments keep dispatching unsigned commands until operators populate trust anchors and flip the flag. Witness rolled with C.11. Before/after transcripts at `testbed/tests/audit/transcripts/e01_mgmt_unauth_{before,after}.txt`. **Update (2026-06-13): the default has since flipped** — `ndn_config`'s `require_signed_commands` now defaults to `true` (`default_require_signed_commands()` in `crates/ndn-config/src/config.rs`), matching `ARCHITECTURE.md`. The "default is still off" clause above is retained for history but no longer describes the code. | 2026-05-01 |
| G.03 | `g03_psync_interop.sh` | RESOLVED 2026-05-28 (RUST-UNIT + DOCKER INTEROP) | IBF cell-selection and `keyCheck` use `MurmurHash3_x86_32` per PSync `detail/util.cpp::murmurHash3` with `N_HASH=3` and `N_HASHCHECK=11`. The interop image now builds upstream C++ PSync, ships a deterministic C++ FullProducer fixture, and ships ndn-rs `ndn-psync-consumer`; the witness brings up the Docker NFD+interop topology and requires five distinct deterministic Sync update prefixes. Local run 2026-05-28 observed user-0 through user-4 from the C++ producer. | 2026-05-28 |
| G.04 | `g04_nlsr_interop.sh` | RESOLVED 2026-05-28 (LIVE-INTEROP + PCAP) | The C++ NLSR sidecar now runs against ndn-rs NLSR over the Docker NFD topology. The witness requires route convergence in both directions: ndn-fwd-nlsr installs the C++ router's `/test/r1/data` prefix, while C++ NLSR/NFD installs ndn-rs's `/test/r2/data` prefix with `origin=nlsr`. A pcap artifact is saved at `testbed/tests/audit/transcripts/g04_nlsr_interop_after.pcap`. | 2026-05-28 |

## MAJOR

| Finding | Witness test | Status | Predicted failure | Last seen |
|---------|------|--------|----|-|
| X.04 | `x04_reliability_feature_canonical.sh` | RESOLVED 2026-05-23 | Reliability consolidated onto the canonical `ReliabilityFeature`. The feature now frames egress via `frame()` (= `LpReliability::on_send`: TxSequence + piggybacked Acks + retx tracking), the send loop calls it when reliability is enabled, and the retx tick pumps `take_retransmissions()` + `take_acks()`. Store C (`FaceState.reliability` / `new_reliable` / pump branches) deleted; discovery UDP faces enable the feature; `inject_packet` feeds `note_receive`. RUST-UNIT both green (`canonical_core_recovers_loss`, `feature_frame_emits_tx_sequence`). See `.claude/notes/per-face-option-wiring-triage-2026-05-23.md`. | 2026-05-23 (rust-unit witness) |
| X.07 | `x07_reliability_udp_loss.sh` | RESOLVED 2026-05-23 (INTEROP-SCRIPT) | End-to-end interop leg for X.04: two `ndn-fwd` containers over a netem-lossy UDP link, LpReliability enabled on both ends of the inter-forwarder face via the new `ndn-ctl face update --flags 0x2`. Under 30% loss/direction the ping burst recovers to ~34/40 (vs ~16/40 baseline) and face A reports `resent>0`. Baseline (before) leg reproducible via `NO_RELIABILITY=1` (16/40, resent=0, exit 1). Requires the `reliability` compose profile + the testclient image bundling `ndn-fwd`+`tc`. Before/after transcripts at `testbed/tests/audit/transcripts/x07_reliability_udp_loss_{before,after}.txt`. | 2026-05-23 (interop-script witness, local docker) |
| A.02 / A.21 | `a02_psdc_structural.sh` | RESOLVED 2026-05-02 | `Interest::decode` now calls a new `validate_psdc_structure` after `Name::decode` and a forked-reader `body_has_app_params` scan; rejects (i) AppParams without PSDC, (ii) PSDC not at the last position, (iii) more than one PSDC. Mirrors ndn-cxx `interest.cpp:171-173,303,692-710` and ndnd `spec_2022/spec.go:513-518`. Witness: RUST-UNIT covering all three shapes. Before/after transcripts at `testbed/tests/audit/transcripts/a02_psdc_structural_{before,after}.txt`. | 2026-05-01 (rust-unit witness) |
| A.03 / A.04 / N.04 | `a03_unknown_critical_tlv.sh` | RESOLVED 2026-05-02 | New body-level structural pass at `Interest::decode` (`validate_interest_body_structure`) and `Data::decode` (`validate_data_body_structure`) tracks a `last_element` cursor (rejects out-of-order or duplicate spec elements per `interest.html` / `data.html`) and rejects unknown critical TLV-TYPEs via the new `is_critical_tlv_type` helper. `MetaInfo::decode` and `SignatureInfo::decode` got the same critical-bit gate. Mirrors ndn-cxx `interest.cpp:171-173,183-300`, `data.cpp:182`, `signature-info.cpp:158`. Witness: 10 RUST-UNIT tests covering A.03 / A.04 in Interest+Data, N.04 in MetaInfo/SignatureInfo, plus non-critical-unknown sanity checks. Before/after transcripts at `testbed/tests/audit/transcripts/a03_unknown_critical_tlv_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| A.10 | `a10_databuilder_build_sig.sh` | RESOLVED 2026-05-01 | `DataBuilder::build()` now routes through the existing `sign_digest_sha256()` fast path so `SignatureValue` is the real `SHA-256(signed region)` per NDN Packet Format §6.3.2. Witness: RUST-UNIT `a10_databuilder_build_emits_real_sha256` in ndn-packet. Before/after transcripts at `testbed/tests/audit/transcripts/a10_databuilder_build_sig_{before,after}.txt`. | 2026-05-01 (rust-unit witness) |
| A.15 | `a15_keylocator_rules.sh` | RESOLVED 2026-05-27 (RUST-UNIT) | `SignatureInfo::decode` enforces KeyLocator presence/absence by SignatureType, and Data/Interest decoders surface the violation eagerly instead of hiding it behind lazy accessors. Witness passed locally 2026-05-27. | 2026-05-27 |
| C.02 | rolled into `c01_ecdsa_verify.sh` | RESOLVED 2026-05-01 | `HmacSha256Verifier` added; reachable via `verify_by_sig_type` for SignatureType code 4. | 2026-05-01 |
| C.03 | rolled into `c01_ecdsa_verify.sh` | RESOLVED 2026-05-01 | `DigestSha256Verifier` added; reachable on the basic `Validator::validate` path (no KeyLocator required). | 2026-05-01 |
| C.06 | `c06_keychain_sigtype_label.sh` | RESOLVED 2026-05-02 | `KeyChain::sign_data` / `sign_interest` now pass `signer.sig_type()` (rather than the hardcoded `SignatureEd25519`) into `DataBuilder::sign_sync` / `InterestBuilder::sign_sync`. New `SecurityManager::install_signer` opens the path for non-Ed25519 signers (HMAC, BLAKE3, YubiKey). Witness: RUST-UNIT pair in `ndn-security` (`c06_sign_data_uses_signer_sigtype_hmac`, `c06_sign_interest_uses_signer_sigtype_hmac`). Sub-fix in the same pass: `SignatureInfo::decode` recognises `ValidityPeriod` (TLV 0xFD, critical) so cert wires don't fail the N.04 strict-criticality gate. Before/after transcripts at `testbed/tests/audit/transcripts/c06_keychain_sigtype_label_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| C.11 | rolled into `e01_mgmt_unauth.sh` | RESOLVED 2026-05-01 | New `Validator::validate_interest` returns the new `InterestValidationOutcome` (Valid / Invalid / Pending) using `verify_by_sig_type`. Two cargo tests in `ndn-security` cover signed-Valid + unsigned-Invalid. | 2026-05-01 |
| C.12 | `c12_mgmt_sign_digest.sh` / `c12_mgmt_sign_key.sh` / `c12_mgmt_dataset_fresh.sh` | RESOLVED 2026-05-28 (RUST-UNIT + INTEROP) | MgmtClient command Interests are signed. DigestSha256 is witnessed by decoding the generated command Interest and checking `SHA-256(signed_region)`; key-backed signing is witnessed by registering a signed route against Docker NFD and verifying the RIB through `nfdc route list`. The follow-up witness proves dataset Interests set CanBePrefix+MustBeFresh so `ndn-ctl route rib-list` sees a freshly registered route instead of a stale cached segment. | 2026-05-28 |
| C.14 | `c14_ndncert_error_names.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | `ErrorCode` enum renamed to canonical NDNCERT 0.3 wiki names: `BadInterest`→`BadInterestFormat`, `BadApplicationParameters`→`BadParameterFormat`, `InvalidSignature`→`BadSignature`, `OutOfTries`→`RunOutOfTries`, `OutOfTime`→`RunOutOfTime`. Numeric codes (1..=9) unchanged. Witness is the Rust round-trip `c14_error_code_canonical_names_and_numbers`. | 2026-05-28 (rust-unit witness) |
| C.16 | `c16_lvs_user_fn_failsafe.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | LVS user-function schemas are now fail-closed rather than grep-proven: the parser flags user-function use, `TrustSchema::from_lvs_binary` rejects enforcement with `UserFunctionsNotSupported`, and direct `LvsModel`/`LvsPolicy` evaluation treats unsupported user functions as non-matching. The fixture would otherwise allow `/123` signed by `/123` if the user-function constraint were ignored open. | 2026-05-28 (rust-unit witness) |
| Validator config | `validator_config_behavior.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | `ConfigPolicy` now models the release-relevant configuration-validator semantics directly: ordered first-match rules, no-match denial, exact KeyLocator-prefix checker, and a hierarchical checker delegating to the trust schema. This is a behavioral witness, not a claim that ndn-rs parses every ndn-cxx `validator.conf` stanza. | 2026-05-28 |
| D.02 | `d02_localhop_scope.sh` | RESOLVED 2026-05-28 (RUST-UNIT + INTEROP) | New `is_localhop_name` helper in `decode.rs`; `check_scope` drops `/localhop` Interests received on a non-local face. The script now relies on Rust behavior plus the live face-scope split test, not source grep. The drop is conservatively wider than NFD's outbound-only `wouldViolateScope` check (refinement to "permit if a local consumer exists" tracked as follow-up). | 2026-05-28 |
| D.03 | `d03_nexthop_faceid.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | New pure helper `next_hop_override(&AnyMap, face_exists)` in `stages/strategy.rs` returns `Forward(face_id)` / `DropFaceGone` / `None`. `StrategyStage::process` consults it before any FIB / strategy work and honours the override (`Action::Send` to the named face) or drops with `DropReason::UnknownFace` when the face is gone — mirroring NFD `daemon/fw/forwarder.cpp:205-218`. Witness is the Rust helper quartet covering absent / forward / drop-face-gone / overflow-u32. | 2026-05-28 (rust-unit witness) |
| D.04 | `d04_pit_aggregation_selectors.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | `PitToken` keyed on `(Name, ForwardingHint)` only; selectors are stored on each `InRecord` (`PitEntry.selector` field removed). `PitCheckStage` aggregates same-Name Interests into a single entry with multiple in-records. `PitMatchStage` does one token lookup per prefix length instead of probing selector combinations; the prefix-walk filters in-records by `selector.can_be_prefix` so a strict-name pending Interest is no longer satisfied by a longer-name Data. Content Store lookup now has explicit D.04 witnesses that stale cached Data misses `MustBeFresh` Interests at both store and engine `CsLookupStage` level, while stale Data may still satisfy non-`MustBeFresh` Interests. | 2026-05-28 |
| D.07 | `d07_pit_token_echo.sh` | RESOLVED 2026-05-02 | New `encode_lp_nack_with_pit_token` helper in `ndn-packet`; new `PacketContext.out_pit_tokens` field parallel to `out_faces` (populated by `PitMatchStage` from each in-record's `lp_pit_token`); `PacketDispatcher::satisfy` now wraps egress Data in `encode_lp_with_headers` with the consumer's token when present; the Nack path uses `ctx.lp_pit_token` to echo on Nack frames. Mirrors NFD `forwarder.cpp:234`. Witness: RUST-UNIT pair (`d07_encode_lp_nack_with_pit_token_emits_token` in ndn-packet, `d07_pit_match_propagates_lp_token_to_out_tokens` in ndn-engine). Before/after transcripts at `testbed/tests/audit/transcripts/d07_pit_token_echo_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| D.09 | `d09_bestroute_nack_retry.sh` | RESOLVED 2026-05-02 (out-record tracking deferred) | `BestRouteStrategy::on_nack` now overrides the trait default and tries another FIB nexthop (excluding the one that nacked, via `nexthops_excluding(in_face)`); propagates the Nack downstream when no other nexthop is available or no FIB entry exists. Mirrors NFD `best-route-strategy.cpp` `afterReceiveNack` → `processNack`. **Deferred:** per-PIT-entry out-record tracking of tried-upstreams; without it, two nexthops that both Nack each other can ping-pong (HopLimit + PIT lifetime + nonce loop detection bound the practical impact). Witness: RUST-UNIT trio in `ndn-strategy::best_route::tests`. Before/after transcripts at `testbed/tests/audit/transcripts/d09_bestroute_nack_retry_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| D.10 / E.06 | `d10_strategy_name_version.sh` | RESOLVED 2026-05-02 | `BestRouteStrategy::strategy_name()` and `MulticastStrategy::strategy_name()` now append `Version(5)` (TLV 0x36) per NFD `best-route-strategy.cpp:56` and `multicast-strategy.cpp:56` (`appendVersion(5)`). `nfdc strategy-choice set` directives that include the `/v=5` suffix now match. Witness: RUST-UNIT pair in `ndn-strategy` (`best_route::tests::d10_strategy_name_ends_with_version_v5`, `multicast::tests::d10_strategy_name_ends_with_version_v5`). Before/after transcripts at `testbed/tests/audit/transcripts/d10_strategy_name_version_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| D.13 | `d13_localhost_unvalidated.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | The blanket `/localhost` skip is removed from `validation.rs`; /localhost Data goes through the same chain walk as any other Data. Mgmt responses use DigestSha256 today (covered by the C.01/C.03 verifier dispatch). Witness is a forged `/localhost/nfd/...` Data packet with bogus DigestSha256 being dropped by `ValidationStage`. | 2026-05-28 |
| E.03 | `e03_extended_modules_unsigned.sh` | RESOLVED 2026-05-02 | New `is_extended_module` predicate (`mgmt_ndn.rs`) classifies modules as NFD-canonical (`faces`, `fib`, `rib`, `cs`, `strategy-choice`, `status`) vs ndn-rs-extended. `effective_require_signed(module, global_flag)` ORs `is_extended_module` into the global flag so `security/*`, `routing/*`, `discovery/*`, `neighbors/*`, `service/*`, `measurements/*`, `config/*`, `log/*` always require signed commands regardless of the operator's `require_signed_commands` setting. The mgmt dispatch site computes `effective_required` per-Interest and feeds it to `authorize_command`. Witness: RUST-UNIT trio `e03_*` in `ndn-fwd::mgmt_ndn::tests` covering classifier, escalation logic, and end-to-end rejection of an unsigned `/localhost/nfd/security/identity-generate`. Before/after transcripts at `testbed/tests/audit/transcripts/e03_extended_modules_unsigned_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| F.01 / F.03 / F.06 | `f01_faceuri_schemes.sh` | RESOLVED 2026-05-02 | New `ndn_transport::ip_face_uri(scheme_base, addr)` helper dispatches `udp4`/`udp6` and `tcp4`/`tcp6` on `addr.is_ipv4()`; `UdpFace` and TCP face wrappers consume it. New `WsDirection { Client, Server }` enum on `WebSocketFace`; `connect` constructor sets `Client`, `from_stream` sets `Server`; `WsDirection::scheme(tls: bool)` returns NFD's `wsclient` / `wsserver` / `wssclient` / `wss` per (direction, TLS) pair. `TlsWebSocketFace` advertises `wss://`. Witness: RUST-UNIT pair across `ndn-transport` (`f01_ip_face_uri_*`) and `ndn-face-native` (`f06_ws_direction_scheme_*`, `f06_websocket_face_uri_distinguishes_client_and_server`). Before/after transcripts at `testbed/tests/audit/transcripts/f01_faceuri_schemes_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| G.02 | `g02_svs_typed_components.sh` | RESOLVED 2026-05-02 | `SvsNode.vector` keyed by canonical `Name` (was `String`). Legacy `merge` / `seq_for` String-based API parses through `Name::from_str` at the boundary so two equivalent Display renderings (typed components, percent-escaping, etc.) canonicalize to the same HashMap bucket. New `merge_names` / `local_name` Name-keyed entry points avoid the round-trip. Mirrors ndn-svs `common.hpp:41` `using NodeID = ndn::Name`. Witness: RUST-UNIT pair `g02_*` in `ndn-sync::svs::tests`. Before/after transcripts at `testbed/tests/audit/transcripts/g02_svs_typed_components_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| G.06 | `g06_swim_vs_autoconfig.sh` | RESOLVED 2026-05-28 (RUST-UNIT + LIVE-INTEROP) | SWIM discovery artifacts remain absent and NDN AutoConfig surfaces are wired. Rust witnesses cover `/localhop/ndn-autoconf/hub`, nfd::Uri content, and FCH URL parsing. The live reference-client leg runs upstream `ndn-autoconfig` from the interop image against ndn-fwd, serves an NDN-FCH fixture returning `nfd:6363`, creates the hub face, and registers `/` plus `/localhop/nfd` routes with `origin=autoconf`. A prerequisite NFD-compat issue was fixed locally: `faces/create` responses now echo `Flags` and `FacePersistency`, which reference FaceCreateCommand clients require before route registration can proceed. The earlier Docker failures were caused by host Docker storage exhaustion; the testbed still caps build jobs and offers `testbed/tools/up-g06-low-memory.sh` for resource-friendly rebuilds. Follow-up coverage can add DNS SRV and successful multicast-stage fixtures, but the release-blocking AutoConfig interop proof now passes. | 2026-05-28 |
| G.09 | `g09_prefix_announcement_consume.sh` | RESOLVED 2026-05-28 (RUST-UNIT + RUST-INTEG) | PrefixAnnouncement is now witnessed end-to-end inside the engine: `TlvDecodeStage` surfaces LP `PrefixAnnouncement` (0x0350) into `ctx.tags`; the self-learning data path validates the signed announcement, installs a route with `origin=130` toward the announcing face, and a later `/learned/item` Interest forwards over that learned route. Negative witnesses keep the strategy gate, tampered-signature gate, and untrusted-key gate fail-closed. | 2026-05-28 |
| N.02 | `n02_lp_reassembly_collision.sh` | RESOLVED 2026-05-28 (RUST-UNIT + LIVE-UDP) | `ReassemblyBuffer.pending` is keyed by `(endpoint_id: u64, seq: u64)` per NFD's `(EndpointId, Sequence)` reassembly key. Packet and engine witnesses cover synthetic overlapping sequences and `FaceAddr`-derived endpoint IDs. The live UDP fixture sends colliding LP fragment sequences from two real socket source addresses into one shared-medium face and proves both packets reassemble independently. | 2026-05-28 |
| N.03 | `n03_lp_header_order.sh` | RESOLVED 2026-05-02 | `LpPacket::decode` now tracks a `last_typ` cursor + `fragment_seen` flag so it rejects (i) duplicate non-repeatable headers, (ii) out-of-order headers, (iii) any header after `Fragment`. Only `Ack` (TLV-TYPE 0x0344) is repeatable; multiple consecutive `Ack` entries still decode. Mirrors ndn-cxx `lp/packet.cpp:128-135`. Witness: RUST-UNIT quartet `n03_*` in `ndn-packet`. Before/after transcripts at `testbed/tests/audit/transcripts/n03_lp_header_order_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| N.04 | rolled into `a03_unknown_critical_tlv.sh` | RESOLVED 2026-05-02 | Unknown critical TLV inside MetaInfo / SignatureInfo body now triggers `MalformedPacket` via the shared `is_critical_tlv_type` gate added in the same A.03/A.04 pass. See A.03 / A.04 / N.04 row above for detail. | 2026-05-02 (rust-unit witness) |
| N.05 | `n05_nack_no_reason.sh` | RESOLVED 2026-05-27 (RUST-UNIT) | `LpPacket::decode` now preserves Nack-header presence separately from the optional `NackReason`; an empty Nack header decodes with `reason=None` rather than `Other(0)`. `Nack::decode` and app-facing Nack errors carry the optional reason. | 2026-05-27 |
| N.06 | `n06_dead_nonce_list.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | `ndn_store::DeadNonceList` holds `(name_hash, nonce)` fingerprints with TTL expiry; default lifetime 6 s matches NFD. The engine now owns a DNL, inserts retiring PIT nonces on Data satisfaction and PIT expiry, purges it on the PIT expiry tick, and consults it before PIT aggregation so a repeated `(Name, Nonce)` after PIT erasure drops as `LoopDetected`. Witness: store `n06_*` tests plus engine `n06_dnl_rejects_nonce_after_satisfied_pit_entry_is_erased`. | 2026-05-28 |
| N.07 | rolled into `d13_localhost_unvalidated.sh` | RESOLVED 2026-05-01 | The decode-stage `check_scope` already covered Data (it keys on `ctx.name`, not packet kind); the audit's "ingress check missing for Data" claim was outdated. RUST-UNIT `n07_is_localhost_name_recognises_prefix` confirms the helper. | 2026-05-01 |
| N.08 | `n08_unsolicited_data_policy.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | The engine's `UnsolicitedDataPolicy` is now behavior-witnessed through `ForwarderEngine`: `DropAll` rejects unsolicited CS admission, `AdmitAll` admits it, `AdmitLocal` admits only local-face Data, and `AdmitNetwork` admits only non-local/broadcast-face Data. Admitted unsolicited Data remains cache-only and still flows through the verified-Data CS gate. | 2026-05-28 |
| N.09 | `n09_multiaccess_nack_policy.sh` | RESOLVED 2026-05-28 (RUST-UNIT + LIVE-UDP) | Nacks are now treated as point-to-point feedback. The dispatcher suppresses locally generated Nacks on multi-access/ad-hoc ingress faces, drops incoming Nacks from multi-access/ad-hoc faces, and skips Nack propagation to downstream PIT in-record faces with shared-medium link types. The live UDP fixture injects an LP Nack from a real socket source address on a shared-medium face and proves it is not propagated to the downstream consumer. | 2026-05-28 |
| N.10 | `n10_command_replay.sh` | RESOLVED 2026-05-28 (RUST-UNIT + BINARY-WIRING) | `check_sig_time` plus `CommandReplayCache` enforce the +/-120s window and strictly-greater per-signer rule per ndn-cxx `ValidationPolicyCommandInterest`. The standalone `ndn-fwd` binary now mounts management with a live replay cache by default, rather than leaving `MgmtHandles.command_replay_cache=None`. Witness: `ndn-mgmt` `n10_*` tests plus `ndn-fwd` `n10_forwarder_default_replay_cache_is_wired`. | 2026-05-28 |
| N.11 | `n11_control_params_binding.sh` | RESOLVED 2026-05-02 | New `resolve_control_parameters` pure helper plus dispatch-boundary check in `run_ndn_mgmt_handler`. CP must appear in exactly one of (Name's 5th component, ApplicationParameters); both-locations rejected with `BAD_PARAMS`. Combined with audit A.09's signed-region calculation (covers Name + AppParams) and A.02's PSDC enforcement (RESOLVED 2026-05-02), a SIGNED command's CP is bound to the signature in either single location; the dual-location path is now explicitly malformed. Witness: RUST-UNIT quartet `n11_*` in `ndn-fwd::mgmt_ndn::tests`. Before/after transcripts at `testbed/tests/audit/transcripts/n11_control_params_binding_{before,after}.txt`. | 2026-05-02 (rust-unit witness) |
| N.12 | `n12_mgmt_response_signing.sh` | RESOLVED 2026-05-28 (RUST-UNIT + LIVE-INTEROP) | `build_mgmt_response_wire(name, content, signer)` plus `MgmtHandles.command_response_signer` emit signer-backed management responses with SignatureType and KeyLocator when wired, with DigestSha256 fallback when no signer is configured. The Docker testbed now boots ndn-fwd with a persistent ECDSA-P256 PIB identity, reference `nfdc status` decodes live management Data, and `ndn-mgmt-response-verify` fetches `/localhost/nfd/cs/config` and verifies the ControlResponse Data signature against the PIB trust anchor. | 2026-05-28 |
| E.05 | `e05_notification_streams.sh` | RESOLVED 2026-05-28 (RUST-UNIT + LIVE-INTEROP) | `NotificationStream<T>` publishes event Data under `<prefix>/<SequenceNumberComponent>` and satisfies prefix Interests with the latest event, matching the NFD notification-stream shape. Unit witnesses cover publisher/subscriber delivery and semantic face events; the Docker live witness mutates `strategy-choice` through `ndn-ctl`, then `ndn-mgmt-notification-fetch` retrieves the resulting `/localhost/nfd/strategy-choice/notifications/seq=<n>` Data from ndn-fwd and verifies the event content. | 2026-05-28 |
| N.13 | `n13_cert_serialize_tlv.sh` | RESOLVED 2026-05-01 | `serialize_cert` reconstitutes the cert as a real NDN Data TLV from `signed_region`+`sig_value`; `deserialize_cert` parses Data and calls `Certificate::decode`. `SecurityManager::issue_self_signed` and `certify` now populate the wire bytes. Before/after transcripts at `testbed/tests/audit/transcripts/n13_{before,after}.txt`. | 2026-05-01 |
| N.14 | `n14_trust_anchor_validity.sh` | RESOLVED 2026-05-28 (RUST-UNIT) | Trust-anchor insertion now rejects expired and not-yet-valid certificates before they enter either the trust-anchor set or certificate cache. The witness covers both `Validator::add_trust_anchor` and `SecurityManager::add_trust_anchor`, while valid anchors are still accepted. PIB anchor writes also fail closed for invalid validity windows. | 2026-05-28 |
| N.15 | `ndn-strategy::measurements::tests::measurements_table_is_bounded_by_capacity` (RUST-UNIT) | RESOLVED 2026-06-13 | `MeasurementsTable` (per-prefix EWMA RTT / satisfaction state read by `MeasuredStrategy`) previously did `entry(name).or_default()` with no eviction, bound, or expiry — every distinct prefix permanently allocated an entry. **Honest scope:** the native data path does **not** currently write the table (the only writers are in `ndn-wasm`), so this is a *latent* unbounded-growth design issue, not a live DoS — but it would become one the moment native RTT feedback is wired, which the dead native table invites. Fixed by bounding to `DEFAULT_CAPACITY = 16_384` prefixes with least-recently-updated eviction (`with_capacity` configurable), so wiring native feedback is safe-by-construction. NFD bounds the analogous `table::Measurements` by per-entry lifetime; we bound table size since the strategy reads a snapshot, not a lifetime-extended record. Related open gap (not this row): native `MeasuredStrategy` has no live RTT input. | 2026-06-13 |

## NOT-WITNESSABLE

No currently tracked NDN-spec compliance rows remain in this bucket after the
C.16 follow-up. New rows should land here only when the finding has no concrete
runtime behavior that can be exercised by Rust unit, integration, interop, or
wire-capture witnesses.

## NDF-relied-upon extensions (regression guards)

These rows are **not** spec-compliance findings. They are deliberate ndn-rs
extensions/divergences that NDF builds on (filed as ndn-rs feature requests F14
and F16; see `ndf-rs` `ndn-rs-feature-requests.md`). Each witness *passes today*
and exists to keep the relied-upon behavior from regressing unnoticed — per the
README "Proof quality" note, a passing regression guard is valid evidence when
paired with a behavioral (RUST-UNIT) witness, which these are. The divergences
are also documented as intentional extensions in
`docs/wiki/src/reference/spec-compliance.md`.

| Entry | Witness test | Status | Behavior pinned | Last seen |
|---------|------|--------|----|-|
| F14 | `f14_implicit_digest_fetch.sh` | RESOLVED 2026-06-10 (RUST-UNIT) | Consumer-API construction `Name::append_implicit_digest` (F14-i, no hand-rolled name surgery); implicit-digest (`ImplicitSha256DigestComponent`) Content-Store lookup matches/mismatches; CanBePrefix content-hash-suffix resolution respects MustBeFresh — the D-40 track-B `<auth>/blocks/<hash>` behaviors. | 2026-06-10 (rust-unit witness) |
| F15 | `f15_subscription_survival.sh` | RESOLVED 2026-06-10 (RUST-UNIT) | Subscription survives face churn (variant 1). **B1 splice:** a stable SubscriptionId rides the 0x230 value (back-compat); on face-down the surviving persistent budget is parked and spliced back on re-expression with the same id; no id → no survival; expired orphans reaped. **B2 upstream re-establish:** on upstream face-down the stale out-record is pruned and a re-expressed subscription re-forwards (not aggregate-suppress); the consumer re-expresses on a staleness window (fresh signature, same id) to recover an unseen upstream flap. | 2026-06-10 (rust-unit witness) |
| F16(i) | `f16i_subscription_persistence.sh` | RESOLVED 2026-06-10 (RUST-UNIT) | SubscriptionRequest TLV 0x230 exact wire framing + per-InRecord persistence state with data-count budgets; persistent attach isolated from a classical PIT entry at the same name. | 2026-06-10 (rust-unit witness) |
| F16(ii) | `f16ii_replay_guard_shared_key.sh` | RESOLVED 2026-06-10 (RUST-UNIT) | ReplayGuard AND-semantics (replay iff every shared anti-replay field agrees) plus the `monotonic=false` shared-key mode (one key across devices: out-of-order seq admitted, exact in-window repeat still rejected). | 2026-06-10 (rust-unit witness) |
| F16(iii) | `f16iii_inner_tlv_hashing.sh` | RESOLVED 2026-06-10 (RUST-UNIT) | `ContentHashTarget::InnerTlvType(364)` delegated hashing: per-face selection of the inner TLV to hash, present→Some / absent→None, over the SHA-256 implicit-digest primitive. | 2026-06-10 (rust-unit witness) |

## RETRACTED 2026-05-01

The following findings from the 2026-04-20 audit were **withdrawn**
during the cross-reference pass against the on-disk reference
implementations. Do not author witness tests for these. The audit
document should be updated to mark them retracted (with the citation
below as the reason). Removed rows are kept here for traceability.

| Finding | Original severity | Reason for retraction | Citation |
|---------|-------------------|-----------------------|----------|
| A.17 | MAJOR | BLAKE3 SignatureType codes 6 and 7 are now officially registered on the NDN TLV registry (yoursunny). ndn-rs wire is fine; only stale comments remain. | `redmine.named-data.net/projects/ndn-tlv/wiki/SignatureType` |
| C.04 | MAJOR | Same as A.17. | same |
| B.03 | MINOR | ndn-cxx `lp/packet.cpp:107-110` also accepts a top-level Interest/Data inside an LpPacket as the implicit fragment. Standard tolerance, not an ndn-rs deviation. | ndn-cxx `lp/packet.cpp` |
| B.04 | MINOR | PitToken 1-32 byte bound IS the spec; ndn-cxx `lp/pit-token.cpp:28-37` defines `LENGTH_MIN=1`, `LENGTH_MAX=32`. ndn-rs matches. | ndn-cxx `lp/pit-token.cpp` |

## When to add a row here

Every new test added under `tests/audit/` must appear in this
file with its expected status. Removing a row means either:

- the finding was resolved (move to "RESOLVED" and note the
  commit and date of the last-seen pass); or
- the finding was retracted after further spec reading (document
  the retraction in the audit document too).
