=== F.01 / F.03 / F.06 BEFORE FIX (2026-05-02) ===

The witness pre-fix is a synthetic transcript: the regression tests
reference `ndn_transport::ip_face_uri` and the `WsDirection` enum, both
of which were added alongside this fix and did not exist on `main`
prior to the commit. Compilation fails with E0432 ("unresolved import
`ndn_transport::ip_face_uri`") and E0433 ("failed to resolve:
`WsDirection`").

Code-level evidence pre-fix:

  $ grep -n 'pub fn ip_face_uri\|pub enum WsDirection' \
        crates/foundation/ndn-transport/src/face.rs \
        crates/faces/ndn-faces/src/net/websocket.rs
  (no hits)

  $ grep -n 'udp4://' crates/faces/ndn-faces/src/net/udp.rs
  ...:103:    fn remote_uri(&self) -> Option<String> {
  ...:104:        Some(format!("udp4://{}:{}", self.peer.ip(), self.peer.port()))
  (hardcoded `udp4://` for both IPv4 and IPv6 peers)

  $ grep -n 'tcp4://' crates/faces/ndn-faces/src/net/tcp.rs
  (hardcoded `tcp4://` for both families)

  $ grep -n 'self.remote_addr.clone' \
        crates/faces/ndn-faces/src/net/websocket.rs
  ...:111:        Some(self.remote_addr.clone())
  (raw remote address with no scheme prefix; tests for client/server
  direction would see no `wsclient://` or `wsserver://` prefix)

The audit text at § F.01 / F.03 / F.06 cites the NFD FaceUri registry
(wiki/FaceUri): `udp4`/`udp6`/`tcp4`/`tcp6` per IP family,
`wsclient`/`wsserver` per direction, `wssclient`/`wss` for TLS variants.
