# Dashboard Extension: Network Coding

`ndn-dashboard-next` treats network coding as an optional ndn-rs extension
surface. The dashboard displays prefix, role, generation parameters, and
capability state. Non-ndn-rs forwarders show the surface as unsupported unless
they implement the matching management dataset.
