# Dashboard Extension: Compute

Compute services appear in dashboard-next as service names plus diagnostics.
The extension registry keeps compute separate from core Engine state so future
service-specific tools can be added without growing the main dashboard model.
