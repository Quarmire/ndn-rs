[package]
name = "waterline-keel"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "The Keel demo: one pylon-7 alert, four verdicts, zero conversions — and the run report is itself a manifest"

[dependencies]
ndn-manifest = { workspace = true }
ndn-render-contract = { workspace = true }

[lints]
workspace = true
